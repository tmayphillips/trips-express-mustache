class Trip {

  constructor(tripId,title,dateDepart,dateReturn,image) {
    this.tripId = tripId
    this.title = title
    this.dateDepart = dateDepart
    this.dateReturn = dateReturn
    this.image = image
  }
}

module.exports = Trip
